<!-- Updated Contact Section for about.astro -->
<div class="contact-section" data-aos="fade-up">
    <h2>Get in Touch</h2>
    <div class="contact-container">
        <a href="mailto:derekdevon17@gmail.com" class="email-btn">
            <i class="fas fa-envelope"></i> Email Me
        </a>
    </div>
    <div class="newsletter-signup">
        <h3>Stay Updated on Future Projects</h3>
        <form id="newsletter-form">
            <input type="email" placeholder="Your email address" required>
            <button type="submit">Subscribe</button>
            <p class="form-message"></p>
        </form>
    </div>
</div>

<style>
    /* Updated Contact Section Styles */
    .contact-section {
        text-align: center;
        margin: 4rem 0 2rem;
        padding: 2rem;
        background: rgba(255, 255, 255, 0.03);
        border-radius: 8px;
    }

    .contact-section h2 {
        color: #FFD700;
        margin-bottom: 2rem;
    }

    .contact-container {
        margin: 0 auto 2rem;
        max-width: 400px;
    }

    .email-btn {
        display: inline-block;
        background: rgba(255, 215, 0, 0.2);
        color: white;
        padding: 1rem 2rem;
        width: 100%;
        border-radius: 4px;
        text-decoration: none;
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 215, 0, 0.5);
        font-size: 1.1rem;
    }

    .email-btn:hover {
        background: rgba(255, 215, 0, 0.3);
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
    }

    .email-btn i {
        margin-right: 10px;
        font-size: 1.2rem;
    }

    .newsletter-signup {
        max-width: 500px;
        margin: 3rem auto 1rem;
    }

    .newsletter-signup h3 {
        color: #FFD700;
        margin-bottom: 1.5rem;
    }

    .newsletter-signup form {
        display: flex;
        gap: 1rem;
    }

    .newsletter-signup input {
        flex: 1;
        padding: 12px 16px;
        border: none;
        border-radius: 4px;
        background: rgba(255, 255, 255, 0.1);
        color: white;
        font-size: 1rem;
    }

    .newsletter-signup button {
        padding: 12px 24px;
        background: #FFD700;
        color: #121212;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: all 0.3s;
        font-weight: bold;
    }

    .newsletter-signup button:hover {
        background: #e6c200;
        transform: translateY(-2px);
    }
    
    .form-message {
        margin-top: 1rem;
        font-size: 0.9rem;
        height: 20px;
    }

    @media (max-width: 768px) {
        .newsletter-signup form {
            flex-direction: column;
        }
        
        .newsletter-signup button {
            margin-top: 0.5rem;
        }
    }
</style>

<script>
    // Simple form handling for newsletter
    document.addEventListener('DOMContentLoaded', function() {
        const form = document.getElementById('newsletter-form');
        const message = document.querySelector('.form-message');
        
        if (form) {
            form.addEventListener('submit', function(e) {
                e.preventDefault();
                const email = form.querySelector('input[type="email"]').value;
                
                // Here you would typically send this to a server
                // For now, just show a success message
                message.textContent = "Thanks for subscribing! We'll be in touch.";
                message.style.color = "#4CAF50";
                form.reset();
                
                // Clear the message after a few seconds
                setTimeout(() => {
                    message.textContent = "";
                }, 5000);
            });
        }
    });
</script>
